# 到货管家设计系统

> OpenDesign project contract · Mobile-first warehouse receiving console

到货管家不是采购商城，也不是通用数据后台。它是仓库人员在手机上连续拍摄包裹面单、核对订单是否到货，并在必要时处理异常的工作台。每个视觉决定都应缩短“拿起包裹 → 拍照 → 确认保存 → 继续下一件”的时间。

## 设计方向：Field Ledger

整体方向称为 **Field Ledger（现场台账）**：像一张可靠、耐读的仓库登记表，而不是带大量渐变和悬浮卡片的营销页。

- 视觉气质：克制、坚固、清楚、可信。
- 主要表面使用纸张白与温和灰绿，深森林绿只用于品牌和关键动作。
- 红色只表示确实未收货、同步失败或必须处理的异常；橙色表示待确认；绿色表示已完成。
- 用边框、对齐和留白建立层级；阴影仅用于浮层、主操作和移动底栏。
- 保留一个有记忆点的动作：手机收货页的相机按钮像一枚横向“仓库盖章”，始终是首屏最强元素。

## 用户与核心任务

主要用户在仓库、走动中、单手持手机，环境可能反光、嘈杂或网络不稳。优先级如下：

1. 拍摄当前包裹面单并得到明确保存反馈。
2. 快速判断某个采购订单是否已经拍照收货。
3. 查找今天或历史到货凭证并修正单号。
4. 了解同步异常；完整管理统计属于次要信息。

收货页的首屏顺序固定为：精简状态 → 拍照主操作 → 本次结果 → 最近到货。统计不得再次排在拍照动作之前。

## 信息架构

移动端固定三个主入口：

- **收货**：同步摘要、相机主操作、本次结果、最近三至五条凭证。
- **订单**：搜索、收货状态筛选、平台筛选、紧凑订单列表。
- **记录**：本机待同步、今天/历史记录、匹配与异常状态。

账户、退出和完整同步信息归入右上角账户入口，不增加第四个底部导航。桌面端保持同样三入口，只把底部导航转换为左侧导航；不得创造第二套功能结构。

## 响应式与触控

采用 CSS 容器/媒体查询和能力查询，不使用 User-Agent 字符串判断设备。

- 320–479px：单列手机布局，内容左右边距 12–16px，底部导航考虑 `safe-area-inset-bottom`。
- 480–767px：单列宽手机/小平板，适当增加留白，不改变主流程。
- 768–1023px：平板布局，内容可放宽但仍使用移动导航。
- ≥1024px：桌面工作台，左侧固定导航、顶部状态区、主内容最大宽度 1280px。
- 所有主要触控目标至少 44×44px；相机按钮至少 64px 高。
- 320px 不允许横向滚动；长订单号与运单号必须可换行或省略并保留可访问名称。

验收视口固定为 320×720、360×800、390×844、430×932 和 1440×900。

## 色彩与语义

颜色必须通过 `frontend/src/styles/tokens.css` 的语义变量使用。

- `--color-brand-*`：品牌和主要操作，不直接代表业务成功。
- `--color-success-*`：已收货、已同步。
- `--color-warning-*`：部分收货、候选匹配、待人工确认。
- `--color-danger-*`：未收货、失败、阻断错误。
- `--color-neutral-*`：文字、边框、背景和非活动状态。

任何状态都必须同时包含文字和图标，不能只靠红绿区分。普通正文对比度目标 ≥4.5:1，大号文字目标 ≥3:1。

## 字体与数字

中文界面使用系统无衬线字体栈，避免额外字体下载影响仓库网络环境。

- 页面标题 24px/1.2，区块标题 18px/1.3。
- 正文 14–15px/1.55，辅助信息不低于 12px。
- 订单号、运单号使用系统等宽字体栈并启用 `tabular-nums`。
- 数量统计使用 `tabular-nums`；数字与标签基线必须一致。
- 不使用全大写英文制造层级；中文短标签优先。

## 间距、表面与密度

使用 4px 基础、8px 主节奏：4、8、12、16、24、32、40。

- 页面级容器圆角 16px，行项目圆角 10–12px，胶囊只用于短状态或筛选。
- 普通内容以 1px 边框和分隔线组织，不套娃卡片。
- 手机订单折叠行目标高度 112–132px；一屏应能扫到至少 4 个订单摘要。
- 到货记录默认行高 84–96px，缩略图 64px；详情按需展开。
- 空状态不应占据整屏，必须保留通往下一步的动作。

## 组件规则

### 导航

使用统一的 20–22px 内联 SVG 线性图标。当前页同时通过图标、文字、颜色和 `aria-current` 表达。手机底栏固定，桌面左栏固定。

### 相机主操作

相机按钮是唯一允许使用明显品牌底色的整块操作。标签必须是动词短语“拍摄包裹面单”，次文案说明会拉起后置相机。处理中禁用并显示明确进度。

### 统计摘要

默认只显示用户语言：总订单、未收货、已收货、待确认。照片数与匹配数可作为辅助说明，不成为主指标。

### 订单行

折叠态只保留平台、订单号、一个商品摘要、一个物流摘要、收货状态。多商品/多包裹通过一个展开入口查看。平台完成状态不能代替仓库拍照收货状态。

### 状态图标

使用 SVG：完成为勾选圆、待确认为时钟/半圆、失败为警示三角、未收货为包裹缺口。禁止依赖 Unicode/Emoji，因为各设备字形不一致。

## 反馈、离线与错误

- 拍照后立即说明“已保存本机”，网络状态不得让用户误以为照片丢失。
- 离线时保留拍照能力，并清楚显示待上传数量。
- 同步失败提供可点击重试，0 失败时不制造红色提示。
- 订单数据必须显示数据来源与最后同步状态；过期数据应说明“可能不是最新”。
- 成功反馈可使用轻振动，但尊重 `prefers-reduced-motion`。

## 动效与可访问性

动效只用于状态变化和页面层级，不用于装饰。

- 常规过渡 120–180ms；禁止长距离弹跳。
- `prefers-reduced-motion: reduce` 时关闭非必要动画。
- 键盘焦点环在浅色和深色表面都必须可见。
- 表单有真实标签；错误使用 `role="alert"`，进度使用 `role="status"`。
- 图标装饰时 `aria-hidden="true"`，图标按钮必须有可读名称。

## 视觉评审与发布门槛

每个版本用相同真实数据、相同五个视口截图，并按 100 分量表评审：核心任务 20、信息架构 12、密度 15、视觉完成度 15、可访问性 15、响应式 13、状态反馈 10。

- V0 基线：68/100。
- V1 目标：≥78，完成移动端层级与密度重排。
- V2 目标：≥85，完成统一图标、视觉令牌和桌面自适应。
- 最终目标：≥90，并通过功能测试、构建和真实部署检查。

若核心拍照/查询流程不可完成、320px 横向溢出、按钮小于 44px，或业务状态仅靠颜色表达，则该版本最高 59 分且禁止发布。
